Changelog:

 * Added Calendar app to the build
 * Added AudioFX support
 * Fixed missing auxiliary camera lens in Aperture
 * Enabled frame pacing for smoother visual performance
 * Cleaned up leftover charging bypass components
 * Removed Charging Control v2 implementation
 * Removed QS tile changes related to charging and info panels
 * Removed Sony Dolby Atmos integration
 * Removed Spatial Audio synchronization changes
 * Removed Dolby Atmos codec and framework support
 * Removed duplicate Dolby audio declarations
 * Removed unused Dolby post-processing audio effects
 * Removed charging bypass support
 * Removed manual charging bypass toggles
 * Removed charging bypass sepolicy rules
 * Removed charging bypass node fixes
 * Removed system app permissions for charging bypass control
 * Disabled blur features
 * Reworked fingerprint HAL and enabled early preload for first-boot SetupWizard experience
 * Adjusted navigation bar padding overlay values
 * Adjusted status bar height overlay values
 * Moved display density configuration from BoardConfig.mk to system.prop
 * Added QTI Health AIDL service support (including recovery)
 * Updated OMX dependencies to align with latest sources
 * Removed redundant fingerprint system property
 * Fixed NFC APEX platform availability check
 * Tuned brightness values to better match stock ROM behavior
 * Removed brightness optimization overlay parameters
